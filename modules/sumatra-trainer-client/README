# Sumatra Client Server
This module creates a client, which is calculating a TrainingSet provided by the server and send the results back.


## Build the sumatra-trainer-server
mvn clean install

## Build the image
NOTE: a docker deamon has to run on the local machine to build the image
mvn docker:build

## (Optional) Create a network 
docker network create sumatra-training-net

## Run the sumatra-trainer-server with docker
docker run -d --name sumatra-trainer-client --network sumatra-training-net -p 8080:8080 sumatra-trainer-client